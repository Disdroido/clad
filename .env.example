# Database (Neon Postgres)
NEON_DATABASE_URL=postgresql://user:pass@ep-xxx.region.neon.tech/clad?sslmode=require

# OpenRouter (for AI - both vision and text models)
OPENROUTER_API_KEY=sk-or-xxx

# Cloudflare R2 (image storage)
# R2_API_TOKEN is a Cloudflare API token (cfat_...) with R2 read/write/edit
# on the bucket. Generate one at:
#   Dashboard -> R2 -> Manage R2 API Tokens -> Create API Token
# R2_PUBLIC_URL is either the bucket's r2.dev URL (e.g. https://pub-xxx.r2.dev)
# or your custom domain (e.g. https://images.example.com).
R2_ACCOUNT_ID=xxx
R2_API_TOKEN=cfat_xxx
R2_BUCKET_NAME=clad-images
R2_PUBLIC_URL=https://pub-xxx.r2.dev

# Auth
AUTH_SECRET=generate-a-random-secret-with-openssl-rand-base64-32

# Public
PUBLIC_APP_URL=http://localhost:3000
