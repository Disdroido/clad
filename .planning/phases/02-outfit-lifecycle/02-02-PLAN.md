---
phase: 02-outfit-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/outfits/[id].vue
  - app/pages/outfits/wear-history.vue
  - app/pages/outfits/index.vue
  - app/layouts/default.vue
autonomous: true
requirements: [WEAR-03, WEAR-05]
user_setup: []

must_haves:
  truths:
    - "User can log wearing an outfit from the outfit detail page"
    - "User can rate an outfit (1-5) from the outfit detail page"
    - "User can see a 'What I Wore' history page with past wears"
    - "User can see wear count on outfit cards in the outfits list"
    - "User can navigate to wear history from main navigation"
  artifacts:
    - path: "app/pages/outfits/[id].vue"
      provides: "Wear log button + star rating UI on outfit detail page"
      min_lines: 140
    - path: "app/pages/outfits/wear-history.vue"
      provides: "Paginated wear history view"
      min_lines: 80
    - path: "app/pages/outfits/index.vue"
      provides: "Wear count display on outfit cards"
    - path: "app/layouts/default.vue"
      provides: "Navigation link to wear history"
  key_links:
    - from: "app/pages/outfits/[id].vue"
      to: "/api/outfits/{id}/wear"
      via: "$fetch POST"
      pattern: "fetch.*wear"
    - from: "app/pages/outfits/wear-history.vue"
      to: "/api/outfits/wear-history"
      via: "$fetch GET"
      pattern: "fetch.*wear-history"
---

<objective>
Build the user interface for wear tracking, outfit rating, and wear history.

**Purpose:** Give users a way to log what they wore, rate their outfits, and look back at their wear history — completing the outfit lifecycle loop.

**Output:**
- Updated outfit detail page with "Log Wear" button + star rating
- New "What I Wore" history page
- Updated outfit cards showing wear count
- Updated navigation with link to wear history
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**API contracts (from Plan 02-01):**

```typescript
// POST /api/outfits/{id}/wear
// Request: { wornDate?: string (ISO), notes?: string }
// Response: { id, outfitId, wornDate, notes, createdAt }

// PATCH /api/outfits/{id}/rating
// Request: { rating: number (1-5) }
// Response: { id: string, rating: number }

// GET /api/outfits/wear-history?limit=20&offset=0
// Response: {
//   events: Array<{
//     id, outfitId, wornDate, notes, rating,
//     outfit: { id, occasion, explanation, items: Array<{id, imageUrl, clothingType, colour}> }
//   }>,
//   total: number
// }

// GET /api/outfits — returns { outfits: Array<{...items, wearCount, lastWornAt}> }
```

**Existing outfit detail page** at `app/pages/outfits/[id].vue`:
- Exposes: `outfit` ref, `loading` ref, `fetchOutfit()`, `archiveOutfit()`
- UI: back link, loading spinner, outfit card with items grid, explanation, archive button
- Currently NO wear tracking, NO rating, NO wear count display

**Existing outfit list page** at `app/pages/outfits/index.vue`:
- Exposes: `outfits` ref, `loading` ref, `fetchOutfits()`
- UI: "Generate" button, loading spinner, grid of outfit cards
- Each card shows: occasion badge, date, item images, explanation
- Currently NO wear count, NO lastWornAt display

**Existing default layout** at `app/layouts/default.vue`:
- Desktop sidebar: Wardrobe, Outfits, Settings (no wear history link)
- Mobile BottomNav: Wardrobe, Outfits, Settings (no wear history link)

**NuxtLink pattern for wear history:** `/outfits/wear-history`
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add wear logging and rating UI to outfit detail page</name>
  <files>app/pages/outfits/[id].vue</files>
  <action>
    Extend the existing outfit detail page at `app/pages/outfits/[id].vue`:

    1. **Add reactive state:**
    ```typescript
    const wearing = ref(false)
    const wearSuccess = ref(false)
    const userRating = ref<number>(outfit.value?.rating || 0)
    const savingRating = ref(false)
    ```

    2. **Add wear count + last worn display** below the explanation text:
    ```html
    <div class="flex gap-4 text-sm text-brand-500 mb-4">
      <span v-if="outfit.wearCount > 0">
        👗 Worn {{ outfit.wearCount }} time{{ outfit.wearCount === 1 ? '' : 's' }}
      </span>
      <span v-if="outfit.lastWornAt">
        Last worn {{ new Date(outfit.lastWornAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) }}
      </span>
      <span v-else-if="outfit.wearCount === 0 || outfit.wearCount === undefined">
        Not worn yet
      </span>
    </div>
    ```

    3. **Add wear + rating buttons section** between the items grid and the archive button:
    ```html
    <div class="flex gap-3 mb-4">
      <button
        @click="logWear"
        :disabled="wearing"
        class="flex-1 rounded-lg bg-brand-600 py-3 font-medium text-white hover:bg-brand-700 transition disabled:opacity-50"
      >
        {{ wearing ? 'Logging...' : wearSuccess ? '✅ Logged!' : '👕 Log as Worn Today' }}
      </button>
    </div>

    <div class="mb-6">
      <p class="text-sm font-medium text-brand-700 mb-2">Rate this outfit</p>
      <div class="flex gap-1">
        <button
          v-for="star in 5"
          :key="star"
          @click="setRating(star)"
          :disabled="savingRating"
          class="text-2xl transition hover:scale-110 disabled:opacity-50"
        >
          {{ (userRating >= star) ? '⭐' : '☆' }}
        </button>
      </div>
    </div>
    ```

    4. **Add handler functions:**
    ```typescript
    async function logWear() {
      wearing.value = true
      wearSuccess.value = false
      try {
        await $fetch('/api/outfits/' + outfit.value.id + '/wear', { method: 'POST' })
        wearSuccess.value = true
        setTimeout(() => { wearSuccess.value = false }, 2000)
        await fetchOutfit() // refresh to get updated wearCount
      } catch { /* ignore */ }
      finally { wearing.value = false }
    }

    async function setRating(rating: number) {
      savingRating.value = true
      userRating.value = rating // optimistic
      try {
        await $fetch('/api/outfits/' + outfit.value.id + '/rating', {
          method: 'PATCH',
          body: { rating }
        })
      } catch {
        userRating.value = outfit.value?.rating || 0 // revert
      }
      finally { savingRating.value = false }
    }
    ```

    5. **Keep the exact same layout structure, styling conventions, and emoji/class patterns** as the existing page.

    **Note:** Wrap the wear/rating section in a conditional `v-if="outfit"` to prevent rendering before data loads.
  </action>
  <verify>
    <automated>grep -c "wearCount\|logWear\|userRating\|Log as Worn" app/pages/outfits/\[id\].vue || echo "Missing wear tracking UI components"</automated>
  </verify>
  <done>
    - Outfit detail page shows wear count and last worn date
    - "Log as Worn Today" button works, shows feedback, refreshes data
    - Star rating component is interactive, saves via API, has optimistic update
    - All states (loading, empty wear count, existing rating) handled
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build wear history page ("What I Wore")</name>
  <files>app/pages/outfits/wear-history.vue</files>
  <action>
    Create a new page at `app/pages/outfits/wear-history.vue`:

    **Script section:**
    ```typescript
    definePageMeta({ layout: 'default' })
    useHead({ title: 'What I Wore — Clad' })

    const events = ref<any[]>([])
    const total = ref(0)
    const loading = ref(true)
    const limit = 20
    const offset = ref(0)
    const loadingMore = ref(false)

    async function fetchHistory(append = false) {
      if (append) loadingMore.value = true
      else loading.value = true
      try {
        const res = await $fetch('/api/outfits/wear-history', {
          params: { limit, offset: offset.value }
        })
        if (append) {
          events.value = [...events.value, ...res.events]
        } else {
          events.value = res.events
        }
        total.value = res.total
      } catch {
        if (!append) events.value = []
      } finally {
        loading.value = false
        loadingMore.value = false
      }
    }

    function loadMore() {
      offset.value += limit
      fetchHistory(true)
    }

    onMounted(() => fetchHistory(false))
    ```

    **Template section:**
    - `<h1 class="text-2xl font-bold text-brand-950 mb-6">What I Wore</h1>`
    - Loading spinner (same pattern as other pages: `animate-spin rounded-full border-4 border-brand-200 border-t-brand-600`)
    - Empty state when `events.length === 0 && !loading`:
      - "No wear history yet. Start logging outfits you wear!"
      - NuxtLink to `/outfits` browsing
    - Events list grouped by date:
      ```html
      <div v-for="(group, idx) in groupedEvents" :key="idx" class="mb-6">
        <h2 class="text-sm font-semibold text-brand-400 mb-3 uppercase tracking-wide">{{ group.date }}</h2>
        <div v-for="event in group.events" :key="event.id" class="mb-3">
          <NuxtLink :to="'/outfits/' + event.outfitId"
            class="block rounded-xl bg-white p-4 shadow-sm border border-brand-100 hover:shadow-md transition">
            <div class="flex items-center justify-between mb-2">
              <span class="rounded-full bg-brand-100 px-2 py-0.5 text-xs font-medium text-brand-700 capitalize">
                {{ event.outfit.occasion }}
              </span>
              <span v-if="event.rating" class="text-sm">⭐ {{ event.rating }}/5</span>
            </div>
            <p class="text-sm text-brand-600 italic mb-2">{{ event.outfit.explanation }}</p>
            <div v-if="event.outfit.items?.length" class="flex gap-2 overflow-x-auto">
              <img v-for="item in event.outfit.items" :key="item.id"
                :src="item.imageUrl" :alt="item.clothingType"
                class="h-16 w-16 flex-shrink-0 rounded-lg border border-brand-100 object-cover" />
            </div>
            <p v-if="event.notes" class="text-xs text-brand-400 mt-2">{{ event.notes }}</p>
          </NuxtLink>
        </div>
      </div>
      ```
    - "Load More" button at bottom when `events.length < total`:
      ```html
      <button v-if="events.length < total"
        @click="loadMore" :disabled="loadingMore"
        class="w-full rounded-lg border border-brand-300 py-3 text-brand-700 hover:bg-brand-50 transition disabled:opacity-50">
        {{ loadingMore ? 'Loading...' : 'Load More (' + (total - events.length) + ' remaining)' }}
      </button>
      ```

    **Grouping helper** (computed property):
    ```typescript
    const groupedEvents = computed(() => {
      const groups: Record<string, any[]> = {}
      for (const event of events.value) {
        const date = new Date(event.wornDate).toLocaleDateString('en-US', {
          weekday: 'long', month: 'long', day: 'numeric', year: 'numeric'
        })
        if (!groups[date]) groups[date] = []
        groups[date].push(event)
      }
      return Object.entries(groups).map(([date, events]) => ({ date, events }))
    })
    ```
  </action>
  <verify>
    <automated>ls app/pages/outfits/wear-history.vue && grep -c "wear-history\|fetchHistory\|Wore\|groupedEvents" app/pages/outfits/wear-history.vue</automated>
  </verify>
  <done>
    - "What I Wore" page exists at `/outfits/wear-history`
    - Shows events grouped by date with outfit details
    - Empty state renders when no wear history
    - "Load More" pagination appends events
    - Page follows existing layout/styling conventions
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Show wear count on outfit cards + navigation link</name>
  <files>
    app/pages/outfits/index.vue
    app/layouts/default.vue
  </files>
  <action>
    **A. Update outfit cards** in `app/pages/outfits/index.vue`:

    Add wear count badge next to the date on each outfit card. Find this section:
    ```html
    <div class="mb-3 flex items-center justify-between">
      <span class="rounded-full bg-brand-100 px-2 py-0.5 text-xs font-medium text-brand-700 capitalize">
        {{ outfit.occasion }}
      </span>
      <span class="text-xs text-brand-400">{{ new Date(outfit.createdAt).toLocaleDateString() }}</span>
    </div>
    ```

    Replace the date span with:
    ```html
    <div class="flex items-center gap-2">
      <span v-if="outfit.wearCount > 0" class="text-xs text-brand-400">
        👗 {{ outfit.wearCount }}
      </span>
      <span class="text-xs text-brand-400">{{ new Date(outfit.createdAt).toLocaleDateString() }}</span>
    </div>
    ```

    **B. Add navigation link** in `app/layouts/default.vue`:

    Add a "What I Wore" link after the "Outfits" link in the desktop sidebar:
    ```html
    <NuxtLink
      to="/outfits/wear-history"
      class="flex items-center gap-3 rounded-lg px-4 py-2.5 text-sm font-medium transition"
      :class="isActive('/outfits/wear-history')
        ? 'bg-brand-100 text-brand-700 font-semibold'
        : 'text-brand-500 hover:bg-brand-50 hover:text-brand-700'"
    >
      <span class="text-lg leading-none" aria-hidden="true">📅</span>
      What I Wore
    </NuxtLink>
    ```

    Place it right after the Outfits nav item, before the Settings divider. Also add to the mobile BottomNav component at `app/components/BottomNav.vue`, but only if it fits within the nav items (currently 3 items). If adding a 4th item makes it crowded, skip the BottomNav update — it's optional, the main nav is the desktop sidebar.

    Also add to the desktop sidebar `navItems` array if one exists, rather than hardcoding the HTML.
  </action>
  <verify>
    <automated>grep -c "wearCount\|What I Wore\|wear-history" app/pages/outfits/index.vue app/layouts/default.vue</automated>
  </verify>
  <done>
    - Outfit cards in the list show wear count (when > 0)
    - Navigation has a "What I Wore" link in the desktop sidebar
    - All links use existing styling conventions
  </done>
</task>

</tasks>

<verification>
1. All pages compile without errors (`npm run dev` or `npx nuxt build`)
2. Outfit detail page renders with wear/rating UI
3. Wear history page loads and groups events by date
4. Navigation link to wear history works
5. All UI uses TailwindCSS v4 classes matching existing conventions
</verification>

<success_criteria>
- Outfit detail page has interactive wear logging + star rating
- "What I Wore" history page exists with grouped view
- Outfit list cards show wear count badges
- Navigation includes link to wear history
- All pages maintain consistent styling with existing app
</success_criteria>

<output>
After completion, create `.planning/phases/02-outfit-lifecycle/02-02-SUMMARY.md`
</output>
